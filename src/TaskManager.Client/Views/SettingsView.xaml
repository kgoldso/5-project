<UserControl x:Class="TaskManager.Client.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid Margin="24">
        <StackPanel Width="400" VerticalAlignment="Top">
            <TextBlock Text="{DynamicResource Settings}"
                       FontSize="22" FontWeight="SemiBold" Margin="0,0,0,24"/>

            <TextBlock Text="{DynamicResource Language}" FontSize="14" Margin="0,0,0,8"/>
            <ComboBox ItemsSource="{Binding AvailableLanguages}"
                      SelectedItem="{Binding SelectedLanguage}"
                      FontSize="14" Padding="8"/>
        </StackPanel>
    </Grid>
</UserControl>
